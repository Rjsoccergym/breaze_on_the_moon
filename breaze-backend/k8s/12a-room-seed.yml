apiVersion: batch/v1
kind: Job
metadata:
  name: room-seed-job
  namespace: breaze
spec:
  ttlSecondsAfterFinished: 300
  backoffLimit: 6
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: room-seed
          image: postgres:16-alpine
          command:
            - sh
            - -c
            - |
              until pg_isready -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" -d "$PGDATABASE"; do
                echo "Esperando a room-postgres..."
                sleep 5
              done

              until [ "$(psql -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" -d "$PGDATABASE" -Atqc "SELECT to_regclass('public.habitaciones') IS NOT NULL;")" = "t" ]; do
                echo "Esperando la tabla habitaciones..."
                sleep 5
              done

              psql -v ON_ERROR_STOP=1 -h "$PGHOST" -p "$PGPORT" -U "$PGUSER" -d "$PGDATABASE" -f /seed/seed.sql
          env:
            - name: PGHOST
              value: room-postgres
            - name: PGPORT
              value: "5432"
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: secret-room
                  key: DB_USER
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: secret-room
                  key: DB_PASSWORD
            - name: PGDATABASE
              valueFrom:
                secretKeyRef:
                  name: secret-room
                  key: DB_NAME
          volumeMounts:
            - name: room-seed-volume
              mountPath: /seed
      volumes:
        - name: room-seed-volume
          configMap:
            name: room-seed-sql
